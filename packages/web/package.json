{
  "name": "@kairos/web",
  "version": "0.1.0",
  "type": "module",
  "description": "Kairos web dashboard — browser UI for casual users",
  "scripts": {
    "build": "tsup",
    "dev": "node dist/server.js",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kairos/core": "*",
    "@kairos/runtime": "*",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
