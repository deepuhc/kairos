{
  "name": "@kairos/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Kairos orchestration engine — scheduler, patterns, state, gates",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kairos/providers": "workspace:*",
    "@kairos/security": "workspace:*"
  },
  "devDependencies": {
    "@kairos/test-utils": "workspace:*",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  }
}
